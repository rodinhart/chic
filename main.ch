∃ Vec x, y, z

A ∈ Vec + B ∈ Vec ≡ Vec A.x + B.x  A.y + B.y  A.z + B.z

A ∈ Vec × B ∈ Vec ≡ Vec
  A.y × B.z - A.z × B.y
  A.z × B.x - A.x × B.z
  A.x × B.y - A.y × B.x

A ∈ Vec · B ∈ Vec ≡ A.x × B.x + A.y × B.y + A.z × B.z

√((Vec 1 2 3) · (Vec 4 5 6))
